# File-Encryptor-Decryp-
Create a tool that reads text content from a file and applies  a simple cryptographic shift (e.g., a Caesar Cipheror XOR  operation) to each character. The program must save the  result to a new file and include a function to decrypt it back  to the original.
